name: Activar inventario de campamento
on:
  push:
    paths:
      - '.github/workflows/camp-inventory-link.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python camp_inventory_patch.py
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add index.html
          git diff --cached --quiet || git commit -m "Añadir gestión de equipo al campamento"
          git push
