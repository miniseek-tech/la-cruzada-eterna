name: Corregir campamento Mision IV final
on:
  push:
    paths:
      - '.github/workflows/fix-campaign-m4-final.yml'
permissions:
  contents: write
jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Corregir progreso Mision IV
        run: |
          python - <<'PY'
          from pathlib import Path
          p=Path('index.html')
          s=p.read_text(encoding='utf-8')
          old="""const completed3=(c.completed||[]).includes(3)||Number(c.mission)>=4||(m3&&m3.resolved&&m3.victory);"""
          new="""const completed3=(c.completed||[]).includes(3)||Number(c.mission)>=4||(m3&&m3.resolved&&m3.outcome==='victory');"""
          if new in s:
              raise SystemExit(0)
          if old not in s:
              raise SystemExit('No se encontro la condicion antigua')
          s=s.replace(old,new,1)
          p.write_text(s,encoding='utf-8')
          PY
      - name: Verificar
        run: |
          grep -q "m3.outcome==='victory'" index.html
          grep -q 'MISIÓN IV · DISPONIBLE' index.html
          grep -q 'mision4.html' index.html
      - name: Publicar
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add index.html
          git diff --cached --quiet && exit 0
          git commit -m 'Corregir estado de victoria y desbloquear Mision IV'
          git push
