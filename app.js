const sections=['menu','campaign','mission1','tavern','placeholder'];const titles={continue:'Continuar partida',bestiary:'Bestiario',heroes:'Héroes',magic:'Magia',equipment:'Equipo',rules:'Reglamento',dice:'Dados'};function show(id){sections.forEach(s=>document.getElementById(s).hidden=s!==id);scrollTo(0,0)}document.querySelectorAll('[data-go]').forEach(b=>b.onclick=()=>{const t=b.dataset.go;if(sections.includes(t))show(t);else{document.getElementById('placeholderTitle').textContent=titles[t]||t;show('placeholder')}});const key='cruzadaEternaProgress';let state={turn:1,tombs:[false,false,false]};try{state={...state,...JSON.parse(localStorage.getItem(key)||'{}')}}catch(e){}const turn=document.getElementById('turn'),tombs=[...document.querySelectorAll('.tomb')];function save(){localStorage.setItem(key,JSON.stringify(state));turn.textContent=state.turn;tombs.forEach((x,i)=>x.checked=!!state.tombs[i])}document.getElementById('turnMinus').onclick=()=>{state.turn=Math.max(1,state.turn-1);save()};document.getElementById('turnPlus').onclick=()=>{state.turn=Math.min(8,state.turn+1);save()};tombs.forEach((x,i)=>x.onchange=()=>{state.tombs[i]=x.checked;save()});document.getElementById('spawn').onclick=()=>{const n=1+Math.floor(Math.random()*3),active=state.tombs.map((x,i)=>!x?i+1:null).filter(Boolean),r=document.getElementById('spawnResult');r.textContent=active.length?`Aparecen ${n} zombi${n>1?'s':''} desde la Tumba ${active[Math.floor(Math.random()*active.length)]}.`:'Las tres tumbas están purificadas. No aparecen zombis.'};save();if('serviceWorker'in navigator)navigator.serviceWorker.register('./service-worker.js');